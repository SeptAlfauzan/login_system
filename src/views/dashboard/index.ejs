<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/assets/css/output.css">
</head>

<body>
    <div class="w-full flex justify-center min-h-screen items-center">
        <div class="w-1/3 flex items-center flex-col gap-3 border border-slate-500 rounded-lg py-10">
            <h1 class="text-2xl w-3/5">Hello <% if (user) { %>
                    <%= user %>
                        <% } %>, welcome back 👋</h1>
            <div class="w-3/5 flex justify-end">
                <form action="/auth/logout" method="post">
                    <button type="submit"
                        class="bg-blue-300 px-4 py-2 rounded-lg hover:bg-blue-500 hover:text-white duration-150 ">Logout
                        🏃‍♀️</button>
                </form>
            </div>
        </div>
    </div>
</body>

</html>